# Print method for "summary.mixmashnet" objects

Print method for "summary.mixmashnet" objects

## Usage

``` r
# S3 method for class 'summary.mixmashnet'
print(x, digits = 3, top_n = Inf, ...)
```

## Arguments

- x:

  A "summary.mixmashnet" object

- digits:

  Number of digits to print

- top_n:

  Show only the top \`top_n\` rows per block (ranked by \|estimated\|).
  Use \`Inf\` to show all rows (default).

- ...:

  Unused
