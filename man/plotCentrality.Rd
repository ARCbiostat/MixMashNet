% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCentrality.R
\name{plotCentrality}
\alias{plotCentrality}
\title{Plot bootstrap centrality with 95\% CIs (nodes or edges)}
\usage{
plotCentrality(
  fit,
  metrics = c("strength", "expected_influence", "closeness", "betweenness",
    "bridge_strength", "bridge_closeness", "bridge_betweenness", "bridge_ei1",
    "bridge_ei2", "bridge_strength_excluded", "bridge_betweenness_excluded",
    "bridge_closeness_excluded", "bridge_ei1_excluded", "bridge_ei2_excluded",
    "edge_weights"),
  title = "Bootstrap Centrality with 95\% CI",
  plot_nonzero_edges_only = TRUE,
  ordering = c("value", "alphabetical", "community"),
  exclude_nodes = NULL,
  standardize = FALSE,
  color_by_community = TRUE,
  edges_top_n = 60
)
}
\arguments{
\item{fit}{A \code{mixMN_fit} object from \code{mixMN()}.}

\item{metrics}{Character vector of metrics to plot. See details.}

\item{title}{Plot title.}

\item{plot_nonzero_edges_only}{Logical; if TRUE, keeps only non-zero observed edges.}

\item{ordering}{One of \code{"value"}, \code{"alphabetical"}, \code{"community"}.}

\item{exclude_nodes}{Optional character vector of node names to exclude.}

\item{standardize}{Logical; if TRUE, z-standardizes observed and CI values (per panel).}

\item{color_by_community}{Logical; color points by community (nodes only).}

\item{edges_top_n}{Integer; keep only the top-N edges by |weight| (when plotting edges).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates lollipop/errorbar plots of bootstrap estimates with 95\% CIs for:
\itemize{
\item node metrics (general): strength, expected_influence, closeness, betweenness
\item node metrics (bridge): bridge_strength, bridge_closeness, bridge_betweenness, bridge_ei1, bridge_ei2
\item node metrics (excluded): bridge_*_excluded
\item edge metrics: edge_weights (top-N by absolute weight, optional)
}

Only one \emph{type} of metrics can be plotted at a time (general OR bridge OR excluded OR edges).
}
